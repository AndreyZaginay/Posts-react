import { AppBar, Toolbar } from '@mui/material'
import React from 'react'

export const Navbar = () => {
  return ( 
    <AppBar color={'primary'} position="static">
    <Toolbar>
        </Toolbar>
    </AppBar>
  )
}